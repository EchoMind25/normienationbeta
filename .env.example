# Normie Nation Environment Variables
# Copy this file to .env and fill in your actual values for local development

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# On Render: Auto-provided by the database service
DATABASE_URL=postgresql://normie:password@localhost:5432/normie

# ============================================
# SECURITY & AUTHENTICATION
# ============================================
# Secret key for JWT token signing (minimum 32 characters)
# On Render: Auto-generated via render.yaml
# CRITICAL: Must be set in production or app will fail to start
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-change-this

# Admin Solana wallet address (44 characters)
# Users with this wallet address will have admin privileges
# Find your wallet address in Phantom/Solflare wallet
ADMIN_WALLET_ADDRESS=

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Base URL for the application (used in password reset emails)
# On Render: Set to https://tryechomind.net
# For local development: http://localhost:5000
APP_URL=http://localhost:5000

# Server port (default: 5000)
# On Render: Set to 10000
PORT=5000

# Node environment (development or production)
# On Render: Set to production
NODE_ENV=development

# ============================================
# EMAIL CONFIGURATION (SendGrid)
# ============================================
# SendGrid API key for sending emails
# Get from: https://app.sendgrid.com/settings/api_keys
# Required for password reset functionality
SENDGRID_API_KEY=

# Verified sender email address
# On Render: Set to support@tryechomind.net
# MUST be verified in SendGrid: Settings > Sender Authentication
SENDGRID_FROM_EMAIL=support@tryechomind.net

# ============================================
# NOTES FOR LOCAL DEVELOPMENT
# ============================================
# 1. Install PostgreSQL locally or use Docker:
#    docker run --name normie-postgres -e POSTGRES_PASSWORD=password -e POSTGRES_USER=normie -e POSTGRES_DB=normie -p 5432:5432 -d postgres
#
# 2. Run database migrations:
#    npm run db:push
#
# 3. (Optional) To test password reset emails locally:
#    - Sign up for free SendGrid account
#    - Verify a sender email address
#    - Create an API key with Mail Send permissions
#    - Add SENDGRID_API_KEY and SENDGRID_FROM_EMAIL to .env
#
# 4. Start the development server:
#    npm run dev
#
# 5. The app will be available at http://localhost:5000

# ============================================
# RENDER DEPLOYMENT NOTES
# ============================================
# The following are auto-configured via render.yaml:
# - DATABASE_URL (from database provisioning)
# - JWT_SECRET (auto-generated)
# - PORT (set to 10000)
# - NODE_ENV (set to production)
# - APP_URL (set to https://tryechomind.net)
# - SENDGRID_FROM_EMAIL (set to support@tryechomind.net)
#
# You MUST manually add in Render dashboard:
# - ADMIN_WALLET_ADDRESS (your Solana wallet)
# - SENDGRID_API_KEY (from SendGrid)
